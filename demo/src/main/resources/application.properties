# 1. Cổng chạy ứng dụng Spring Boot
server.port=8080

# 2. Chuỗi URL kết nối tới đúng database cinemaManager
spring.datasource.url=jdbc:mysql://localhost:3306/cinemaManager?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&characterEncoding=UTF-8

# 3. Tài khoản & Mật khẩu đăng nhập MySQL
spring.datasource.username=root
spring.datasource.password=anhhung12

# 4. Cấu hình JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect